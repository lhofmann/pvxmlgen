language: python
python:
  - "3.6"
install:
  - pip install pytest
  - pip install pytest-cov
  - pip install coveralls
script:
  - coverage run --include=pvxmlgen.py,xml_state.py -m pytest
  - coverage report
after_success:
  - coveralls
